// Expense Form Submit Event
document.getElementById('expenseForm').addEventListener('submit', function (e) {
    e.preventDefault();
    
    // Get form input values
    const expenseAmount = document.getElementById('expenseAmount').value;
    const expenseCategory = document.getElementById('expenseCategory').value;
    const expenseDescription = document.getElementById('expenseDescription').value;
    const expenseDate = document.getElementById('expenseDate').value;
    
    // Perform validation and send data to the backend
    // You can use AJAX or fetch API to send data to the backend
  });
  
  // Income Form Submit Event
  document.getElementById('incomeForm').addEventListener('submit', function (e) {
    e.preventDefault();
    
    // Get form input values
    const incomeAmount = document.getElementById('incomeAmount').value;
    const incomeCategory = document.getElementById('incomeCategory').value;
    const incomeDescription = document.getElementById('incomeDescription').value;
    const incomeDate = document.getElementById('incomeDate').value;
    
    // Perform validation and send data to the backend
    // You can use AJAX or fetch API to send data to the backend
  });
  
  // Budget Form Submit Event
  document.getElementById('budgetForm').addEventListener('submit', function (e) {
    e.preventDefault();
    
    // Get form input values
    const budgetPriority = document.getElementById('budgetPriority').value;
    const budgetCategory = document.getElementById('budgetCategory').value;
    const budgetAmount = document.getElementById('budgetAmount').value;
    
    // Perform validation and send data to the backend
    // You can use AJAX or fetch API to send data to the backend
  });
  
  // Goal Form Submit Event
  document.getElementById('goalForm').addEventListener('submit', function (e) {
    e.preventDefault();
    
    // Get form input values
    const goalPriority = document.getElementById('goalPriority').value;
    const goalCategory = document.getElementById('goalCategory').value;
    const goalTargetAmount = document.getElementById('goalTargetAmount').value;
    const goalCurrentAmount = document.getElementById('goalCurrentAmount').value;
    const goalTargetDate = document.getElementById('goalTargetDate').value;
    
    // Perform validation and send data to the backend
    // You can use AJAX or fetch API to send data to the backend
  });
  